var sign_in = $('#sign-in-modal');

<% if resource.errors.empty? %>
    sign_in.foundation('reveal', 'close');
    <%= render partial: 'layouts/status' %>
<% else %>
    $('#js-message').html("<%= escape_javascript(render partial: 'layouts/messages') %>");
<% end %>